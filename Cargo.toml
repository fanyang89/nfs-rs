[package]
name = "nfs-rs"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
async-trait = "0.1"
nfsserve = "0.10"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
